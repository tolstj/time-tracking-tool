import React from 'react';
import { Col } from 'antd';

export function TotalHours(): JSX.Element {
  // const selectedMonth = useAppSelector(selectSelectedMonth);
  // const hoursWorkedInMonth = useAppSelector(selectHoursWorkedInMonth);

  return (
    <Col>
      
    </Col>
  );
}
